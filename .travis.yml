dist: trusty
sudo: false

language: node_js
node_js:
  - "12"

branches:
  only:
    - master

addons:
# apt:
#   sources:
#     - google-chrome
#   packages:
#     - google-chrome-stable

cache:
  directories:
    - ./node_modules

install:
  - npm install

script:
  # Use Chromium instead of Chrome.
  # - export CHROME_BIN=chromium-browser
  - npm run lint
